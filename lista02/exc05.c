// 5. Crie um algoritmo que aproxime a raiz quadrada de um número positivo N utilizando o
// Método Babilônico (ou Método de Heron). O usuário deve fornecer N e a precisão desejada
// epsilon (por exemplo, 0.0001). A regra de iteração é:
// xnovo = (xanterior + N / xanterior) / 2
// começando com x = N / 2. O algoritmo deve parar e exibir o resultado quando a diferença
// absoluta entre as duas últimas aproximações for menor que epsilon.

#include <stdio.h>

